# Native visual check — 20 September 2026

These PNGs are real captures of Machogs Design, using this Mac’s read-only scan. They are not the HTML concepts or generated artwork.

- overview-dark.png: completed scan, 46 protected findings, zero cleanup suggestions, memory warning visible.
- protected-dark.png: expanded owning-app explanation, with CPU snapshot and no close control.

The small footer was made more readable after these captures. The rebuilt light appearance was then inspected on screen in its loading state: heading, controls, evidence strip, and sidebar were not clipped. That light image could not be saved after the native bridge began failing again. Command-R was observed entering refresh and returning to a completed result. No real process was closed or cache cleared.

An independent visual reviewer found no layout blocker in the dark screenshots. Further light result-state, keyboard traversal, and Ports interaction coverage is not claimed.
